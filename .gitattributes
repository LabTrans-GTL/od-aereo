# Arquivos criptografados grandes - usar Git LFS
*.parquet.encrypted filter=lfs diff=lfs merge=lfs -text

# Arquivos de backup grandes
*.backup filter=lfs diff=lfs merge=lfs -text

# Executáveis
*.exe filter=lfs diff=lfs merge=lfs -text
